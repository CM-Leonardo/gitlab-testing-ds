import { apiBuscaProjetos } from "../../functions/api/api-comandos"

describe('Deve logar na API e fazer a exclusão de todos os projetos criados', () => {
    it('Excluindo projetos via API', () => {
        apiBuscaProjetos()
    })
})